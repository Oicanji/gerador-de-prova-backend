PORT=3000
ADMIN_PASSWORD=admin123 #senha meme padrão, trocar no render
SESSION_SECRET=admin123
WORK_DIR=./data/work
API_KEYS_FILE=./data/api-keys.json
MAX_QUANTIDADE=30
JOB_TTL_MS=3600000
CORS_ORIGIN=

# Producao (Render)
# URL: https://gerador-de-prova-backend.onrender.com/
# Health: GET /health
# Admin: GET /admin (usuario adm, senha ADMIN_PASSWORD)
# API: POST /api/v1/jobs (header X-API-Key, multipart file + quantidade)
